[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bokurobo"
version = "0.1.0"
description = "「ぼくが考えた変形ロボ」ショート動画生成パイプライン"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
